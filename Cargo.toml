[package]
name = "checklister"
version = "0.1.0"
edition = "2021"

[dev-dependencies]
criterion = "0.6.0"

[dependencies]
chrono = { version = "0.4.40", features = ["serde"] }
eframe = "0.31.0"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0.139"

[[bench]]
name = "hash-bench"
harness = false